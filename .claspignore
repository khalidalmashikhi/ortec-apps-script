# Developer tooling — must never reach the Apps Script project.
tools/**
.git/**
.gitignore
.claspignore
**/*.md

# Walif Coffee is a separate Apps Script project (see walif-coffee/README.md) — never push it into OrTec.
walif-coffee/**
